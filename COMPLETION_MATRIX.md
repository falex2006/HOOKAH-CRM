# Матрица приёмки CRM «Территория»

Документ отделяет фактически подтверждённые результаты от этапов, для которых нужен целевой VPS.

| Область цели | Состояние | Подтверждение |
|---|---|---|
| Премиальная тёмная Velora-тема и единый shell | Подтверждено локально | Браузерная проверка `/admin?qa=final-ui` и `/orders?qa=underline128`; общие `style.css`, `portal.js`, Tabler Icons |
| Фотографическая login-анимация и переход после входа | Подтверждено браузером | `/login?qa=live-login` и `/login.html?qa=live-final`: фотографический фон, 5-секундный переход с дымом/светом, «Пропустить», фирменная форма и открытие `/admin` |
| Desktop/mobile адаптивность | Подтверждено локально | `local-route-smoke.ps1`, UI-проверка на 382–390 px без горизонтального переполнения |
| Рабочее место персонала, столы и заказы | Подтверждено локально | Сквозной сценарий: стол → товар → передача в работу → смешанная оплата → закрытие |
| Брони, VIP-минимумы и скидки | Подтверждено локально | `smoke-test.ps1`, CRUD-контракт и финансовые проверки; VIP 1 500 ₽ / 2 500 ₽ |
| Гости, сотрудники, телефоны, Telegram, аватары и роли | Подтверждено локально | `AUTH_REQUIRED role test: PASS` |
| Визуальная карточка сотрудников и форма телефонов | Подтверждено браузером | `/admin?qa=staff-final#staff`: выравнивание, статусы и компактные действия |
| Финансы, склад, каталог, доставка и аудит | Подтверждено локально | `local-crud-contract.mjs`, smoke-сценарии и локальная приёмка |
| Инфографика и персональные настройки | Подтверждено контрактами и ролями | `local-insights-contract.mjs`, `local-preferences-contract.mjs`, `local-role-contract.mjs`, server-backed dashboard/insights/lock preferences and analytics permission smoke |
| Кликабельность меню, кнопок и форм | Подтверждено локально и браузером | `local-click-contract.mjs` (113 ссылок, 150 кнопок, 4 формы); проверены переходы меню, KPI, настройки инфографики, карточка сотрудника, вкладки и модальные окна |
| Полный локальный приёмочный прогон | Подтверждено локально | Свежий запуск `scripts/local-acceptance.ps1 -BaseUrl http://localhost:3044` 22.09.2026 — `LOCAL ACCEPTANCE: PASS`; после усиления production-secret preflight проверены маршруты, ресурсы, роли, lock/preferences, SaaS, каталог, залы, CRUD, гости, 10 заказов, финансы, доставка и аудит |
| Защита локальных файлов и production-пакета | Подтверждено локально | `local-static-boundary.mjs`, `local-deploy-contract.mjs`; production-шаблон не содержит личного email, preflight требует секреты платформенного владельца |
| Визуальные регрессии CSS, action-ссылок и иконок | Подтверждено локально | `local-design-contract.mjs` — актуальный `style.css?rev=182`, без подчёркиваний, с focus-state и встроенными Tabler Icons |
| Выпадающие меню и сортировки | Подтверждено браузером | Единый custom-select на portal-страницах: выбранное состояние, клавиатура, Escape, клик вне меню и отсутствие дублей в accessibility-дереве; проверено на `/orders` |
| Открытие и закрытие смены администратором | Подтверждено кодом и контрактами | Тематическое CRM-окно с пояснением, проверкой суммы, отменой и Escape; API-операции смены проходят локальный acceptance |
| Редактирование залов, VIP-комнат и сети | Подтверждено кодом | Тематические формы с описанием последствий вместо системных prompt/confirm; сохранение выполняется через существующие API и аудит |
| Браузерный визуальный QA основных экранов | Подтверждено локально | `/admin?qa=visual-next`, `/orders?qa=visual-orders`, `/inventory?qa=visual-inventory` |
| Зависимости | Подтверждено локально | `npm ci --ignore-scripts`, `npm audit --omit=dev --audit-level=high` — 0 уязвимостей |
| Автоматическое продолжение после лимитов | Подтверждено в Codex | Активный heartbeat `crm`, описание в `HANDOFF.md` |
| PostgreSQL-сохранность после перезапуска | Не подтверждено на этой машине | `scripts/local-postgres-contract.mjs` подготовлен для работающего PostgreSQL-контура; локального Docker-контура нет, нужен целевой VPS |
| Ошибки сохранения профиля сотрудника | Подтверждено локально | Ошибка обновления `permissionScopes` больше не подавляется; API возвращает `staff_profile_save_failed`, acceptance проходит |
| Управление залами, этажами и VIP-комнатами | Подтверждено локально | `local-floor-management-contract.mjs`: создание, изменение депозита/вместимости, валидная вместимость в floor API, защита непустой зоны и удаление; полный acceptance проходит |
| Реальный backup restore | Не подтверждено на этой машине | `verify-backup.sh` готов, но нет production-архива и локального Docker-контура; проверка выполняется на целевом VPS |
| VPS, домен, HTTPS и боевые секреты | Не выполнено | Финальный внешний этап; использовать `DEPLOYMENT.md` и `nginx/https.conf.example` |
| Sites-тестовая публикация | Подтверждено | Owner-private deployment `appgdep_6ab2c00ad8b481919a106753a4faa824` succeeded; URL сохранён в `TRACEABILITY.md` |
| Post-deploy smoke-проверка | Подготовлено, не выполнено | `post-deploy-acceptance.sh`; запускать после появления VPS, HTTPS и боевой учётной записи |
| Блокировка рабочего места и PIN-разблокировка | Подтверждено локально и браузером | В рабочем месте открываются настройки интервала 1/5/10/15/30 минут или отключения; ручная блокировка показывает стеклянно-ледяной экран с клавиатурой, корректный PIN возвращает в рабочее место; `local-lock-contract.mjs` и полный acceptance |
| Регрессии на GitHub | Подтверждено | `.github/workflows/crm-contracts.yml`; run `35651251737` для `f83adbb` завершён успешно, включая локальный API для role/insights/date-контрактов |

## Следующий запуск

1. Выполнить локальные контракты из `HANDOFF.md`.
2. На целевом VPS создать `.env` с уникальными секретами и `STAFF_PASSPORT_KEY`.
3. Запустить `./deploy-vps.sh`, затем проверить HTTPS, healthcheck, вход по ролям и backup restore.
4. После каждой внешней проверки обновить эту матрицу и `STATUS.md`.

- Числовые поля и системные спиннеры | Подтверждено локально | CSS rev=177, единый тёмный стиль для staff/Velora

- SaaS multi-tenant foundation | Подтверждено локально | `migrations/009_saas_foundation.sql`, `/api/saas/account`, `local-saas-contract.mjs`; организация, тариф, лимиты и активное использование

- Фильтры очереди рабочего места | Подтверждено локально | Семантические кнопки, клавиатурное управление, aria-pressed

- Переключение зон рабочего места | Подтверждено локально | tablist/tab, aria-selected и сохранённая логика выбора зоны

- Навигация рабочего места | Подтверждено локально | Активный пункт меню синхронизирован с aria-current=page

- Кэширование административной логики | Подтверждено локально | Все portal HTML используют portal.js rev=173, stale-cache проверяется контрактом

- Кэш рабочего места | Подтверждено локально | index.html app.js rev=109 проверяется design contract

| Мобильная боковая навигация | Подтверждено локально | На узкой ширине иконки сохраняют title/aria-label с названием раздела; проверено статическим контрактом и JS-синтаксисом (portal.js rev=173) |

| Согласованность статуса смены | Подтверждено браузером | На главной глобальный статус остаётся в шапке, карточка контроля показывает состояние кассы без повторяющейся формулировки (portal.js rev=173) |

| Мобильная карточка выручки | Исправлено и проверено браузером | На viewport 390×844 блок ожидания не перекрывает сумму; мобильная раскладка вертикальная, CSS rev=182 |

| Планшетная шапка | Исправлено и проверено браузером | При 768×1024 второстепенные поля скрываются без обрезанного имени; основные действия остаются видимыми (CSS rev=182) |

| Реальный healthcheck базы | Подтверждено локально | /api/health возвращает database=memory в demo и 503/degraded при недоступном PostgreSQL; deploy-контракт продолжает проходить |

| Ошибки сохранения гостей | Подтверждено локально | PostgreSQL create/update paths возвращают ошибку вместо ложного успеха; полный acceptance проходит |

| Ошибки сохранения настроек заведения | Подтверждено локально | VIP-минимумы и реквизиты возвращают 503 при сбое БД и восстанавливают memory-состояние; acceptance проходит |

| Надёжность SaaS и бонусов | Подтверждено локально | Ошибки записи подписки и бонусов гостей возвращают 503 и откатывают memory-состояние; полный acceptance проходит |

| Сохранение организаций Control Center | Подтверждено локально | PATCH организации валидирует данные, обновляет PostgreSQL и лимиты подписки, возвращает 503 при ошибке; acceptance проходит |

| Атомарность изменений организации | Подтверждено локально | PATCH Control Center использует BEGIN/COMMIT/ROLLBACK для организации и подписки; acceptance проходит |

| Проверка пакета деплоя | Подтверждено локально | Compose-конфигурация и shell-сценарии проходят синтаксическую проверку; фактический VPS/backup restore ещё требуют целевой среды |

| Контракт Control Center организаций | Подтверждено локально | Проверяются invalid_plan, безопасный PATCH и итоговые поля; включён в полный acceptance |

| Визуальная приёмка финансов и склада | Подтверждено браузером | Desktop 1440×900 и mobile 390×844: карточки, CTA, сетка и custom dropdowns отображаются без перекрытий |

| Безопасность npm-зависимостей | Подтверждено локально | npm ci и npm audit --omit=dev --audit-level=high завершены успешно, уязвимостей не найдено |
| Управление задачами | Подтверждено локально и API smoke-тестом | `020_tasks.sql`, `/api/tasks`, `scripts/tasks-qa.mjs`: создание, список и смена статуса |
| API-антифлуд и антибот | Подтверждено локально | `scripts/security-qa.mjs`: honeypot, лимит 2 MiB, ответ `429` после 180 запросов/IP |
| Единый период аналитики гостей | Реализовано и развёрнуто | `/api/clients?days=7|30|90|365` фильтрует визиты и оборот; интерфейс гостей получил общий переключатель периода |
