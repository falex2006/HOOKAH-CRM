{
  "name": "hookah-crm",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "pg": "^8.13.1"
  },
  "scripts": {
    "start": "node server.js",
    "db:migrate": "node scripts/migrate.js"
  },
  "engines": {
    "node": ">=20"
  }
}
