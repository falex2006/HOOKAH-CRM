name: CRM contracts

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  static-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Validate visual and security contracts
        run: |
          node scripts/local-design-contract.mjs
          node scripts/local-static-boundary.mjs
          node scripts/local-deploy-contract.mjs
          node scripts/local-role-contract.mjs
          node scripts/local-insights-contract.mjs
          node scripts/local-date-contract.mjs
          node scripts/local-login-contract.mjs
      - name: Validate shell scripts
        run: |
          bash -n deploy-vps.sh
          bash -n migrate-vps.sh
          bash -n backup-postgres.sh
          bash -n verify-backup.sh
          bash -n post-deploy-acceptance.sh
      - name: Dependency audit and package check
        run: |
          npm audit --omit=dev --audit-level=high
          npm pack --dry-run
