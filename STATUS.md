# Статус CRM «Территория»

Дата проверки: 2026-09-22

## Продолжение проверки после управления залами

- Добавлено управление зонами и объектами зала: создание/переименование/удаление пустых зон, создание/редактирование/удаление объектов, вместимость и минимальный VIP-депозит. PostgreSQL и memory/demo API приведены к одинаковому поведению.
- Исправлена согласованность memory floor API: вместимость обычных столов и VIP-комнат теперь возвращается тем же полем, что и в PostgreSQL; карточки «Залы и VIP-комнаты» больше не показывают «0 мест». Контракт проверяет валидную вместимость всех объектов.
- Исправлено подавление ошибок при сохранении `permissionScopes` сотрудника в PostgreSQL: сбой теперь возвращается как ошибка профиля, а не маскируется успешным ответом. Полный acceptance после изменения прошёл на свежем сервере.
- Добавлен `scripts/local-floor-management-contract.mjs`: проверяет создание зоны и VIP-комнаты, изменение параметров, защиту удаления непустой зоны и удаление после очистки.
- Полный `scripts/local-acceptance.ps1 -BaseUrl http://localhost:3014` после изменения прошёл: новый floor-management contract и все существующие проверки PASS.

## Подтверждено

- Повторный полный локальный acceptance после усиления production-secret preflight 22.09.2026 на `http://localhost:3044`: все контракты и сквозные сценарии завершились `PASS`, включая deploy contract с обязательными данными платформенного владельца.

- Production-секреты очищены от личных данных 22.09.2026: fallback и `.env.example` используют нейтральный `platform-owner@example.com`, а `deploy-vps.sh` требует боевые `SAAS_OWNER_EMAIL` и `SAAS_OWNER_PASSWORD`; deploy contract и `node --check server.js` проходят.

- Свежий полный локальный приёмочный прогон 22.09.2026 на чистом серверном процессе (`http://localhost:3043`): static boundary, click/design/deploy, маршруты и ресурсы, роли, аналитика, даты, login/lock/preferences, SaaS и организации, onboarding, каталог, залы, CRUD, гости, 10 заказов, smoke и аудит — все проверки `PASS`.

- Браузерная визуальная проверка блокировки рабочего места 22.09.2026: в рабочем месте сотрудника открываются настройки интервала, ручная кнопка «Заблокировать» показывает стеклянно-ледяной экран с цифровой PIN-панелью, а ввод корректного PIN возвращает в рабочее место без перезагрузки.

- Полный локальный аудит кликабельности 22.09.2026: все 99 статических ссылок, 118 статических и динамических кнопок и 3 формы имеют явный маршрут/обработчик и явный `type`; навигация админ-портала, KPI, настройки инфографики, карточка сотрудника, вкладки и модальные окна проверены браузером. Добавлен `scripts/local-click-contract.mjs` в локальный acceptance и GitHub Actions.

- Визуальная проверка `/orders?qa=underline128` 22.09.2026: ссылка «Открыть рабочее место» отображается без стандартного подчёркивания; после сворачивания редактора схемы зала и усиления focus-state CSS cache-busting обновлён до `rev=177`.

- Повторная локальная проверка 22.09.2026: 13 маршрутов и `/api/health` отвечают корректно; role, insights, date, login и static-boundary контракты проходят. Полный acceptance ранее подтверждён; VPS и реальное восстановление PostgreSQL остаются финальным внешним этапом.
- Дополнительная локальная проверка 22.09.2026: CRUD-контракт PASS (товар с фото, склад, финансовая категория, бронь, доставка, точка сети и сотрудник); `docker compose config` PASS; `npm pack --dry-run` PASS (112 файлов).
- Production-проверка 22.09.2026: `deploy-vps.sh`, `backup-postgres.sh`, `verify-backup.sh` и `migrate-vps.sh` проходят `bash -n`; локального Docker-контура на рабочей машине нет, поэтому фактическая сборка образа и восстановление PostgreSQL выполняются только на целевом VPS.
- Production-preflight усилен: deploy и миграции теперь проверяют Docker Compose plugin и обязательный `STAFF_PASSPORT_KEY`, а также отклоняют `replace-*` placeholders; bash syntax, static boundary, date contract и compose config повторно прошли.
- Подготовлен `nginx/https.conf.example` с редиректом HTTP→HTTPS, TLS 1.2/1.3, HSTS и проксированием CRM; локальная машина не содержит nginx/сертификатов, поэтому TLS-конфигурация требует проверки на целевом VPS.
- Добавлен `scripts/local-deploy-contract.mjs` в общий acceptance: проверяет preflight секретов, healthcheck, закрытый порт PostgreSQL и HTTPS-шаблон; локальный контракт проходит.
- Проверка зависимостей 22.09.2026: `npm ci --ignore-scripts --no-audit --no-fund` завершилась успешно, `npm audit --omit=dev --audit-level=high` сообщил 0 уязвимостей.
- Свежая браузерная визуальная проверка `/admin?qa=final-ui` 22.09.2026: Velora-тема, KPI, инфографика, навигация и действия отображаются согласованно; горизонтального переполнения на desktop-экране не обнаружено.
- Браузерная проверка `/login?qa=live-login` 22.09.2026: фотографический hookah-фон, живой 5-секундный smoke/light transition после входа, рабочая кнопка «Пропустить» и переход в `/admin` подтверждены.
- Финальный локальный quality gate после login QA: `node --check` для server/app/portal/login/db, deploy, insights, date и static contracts — PASS; GitHub SHA синхронизирован.
- Браузерная проверка `/admin?qa=staff-final#staff` 22.09.2026: карточки сотрудников, статусы, компактная кнопка блокировки и форма телефонов отображаются ровно, без горизонтального переполнения.
- Повторный локальный `AUTH_REQUIRED` security smoke 22.09.2026 завершён с `AUTH_REQUIRED role test: PASS`: проверены scoped permissions, developer read-only, analytics, logout, блокировка и архивирование сотрудников.

- После визуальных правок обновлены cache-busting версии `style.css` до `rev=177` во всех рабочих HTML-страницах; браузер не должен использовать старый CSS из кэша.

- Локальный asset-smoke 21.09.2026: 13 маршрутов и 18 подключаемых CSS/JS/SVG-ассетов возвращают HTTP 200; проверка включена в единый acceptance-прогон.
- Локальная проверка отдачи страниц 21.09.2026: все 13 маршрутов возвращают HTTP 200 и подключают актуальный runtime (`portal.js`, `app.js` или `login.js`).
- VPS-пакет дополнительно проверен: Docker-образ теперь содержит `scripts/` и `migrations/`, поэтому `npm run db:seed-menu` и миграционные операции доступны внутри контейнера; проверка support-файлов прошла.
- Production packaging check 21.09.2026: Dockerfile now includes all 21 runtime files, including every administrative route and `catalog-seed.js`; static asset check passed. Образ будет собран на целевом VPS: локальный Docker-контур отсутствует.
- Единый локальный приёмочный прогон `scripts/local-acceptance.ps1`: PASS — 13 маршрутов, 24 ТТК, 75 товаров, гости, 10 заказов, VIP/скидки/оплаты, отчёты, доставка и аудит. Нагрузочный прогон на 100 заказов выполняется отдельно через `scripts/local-100-orders.ps1 -Count 100`.
- Браузерная проверка административного справочника 21.09.2026: страница «Категории финансов» открывает форму через «Категория», сохраняет новую категорию и обновляет счётчик списка; подтверждено сообщение «Категория сохранена».
- Браузерная проверка рабочего места 21.09.2026: свободный стол → Red Bull → «Отправить на бар» → статус «Готовится» → смешанная оплата 100 ₽ наличными + 150 ₽ картой; заказ закрывается, редактирование закрытого заказа блокируется.
- Локальная UI-проверка 21.09.2026: основные маршруты (`/`, `/admin`, `/clients`, `/inventory`, `/finance`, `/finance/categories`, `/finance/report`, `/reservations`, `/orders`, `/integrations`, `/network`, `/delivery`) открываются через `localhost`; на мобильной ширине 382 px горизонтального переполнения не обнаружено. Внешние адреса и VPS в этом прогоне не использовались.
- Smoke дополнен сквозными сценариями передачи заказа по статусам и доставки (`new → in_delivery → delivered`); локальный прогон проходит.

- Рабочий web-интерфейс персонала со столами и каталогом.
- Вложенные административные маршруты загружают общие ресурсы и сохраняют тёмную тему; VIP-баннер учитывает только выбранную VIP-комнату.
- Ролевые разрешения owner/admin/senior_bartender/senior_hookah_master/bartender/hookah_master/developer.
- Владелец может назначать управляющему направления доступа (заказы, бронирования, склад, финансы, сотрудники, доставка, интеграции, настройки); ограничения применяются сервером, UI и persisted-сессией.
- Каталог с мультиязычными алиасами поиска.
- Создание заказа, добавление позиций и разделение заказа; рабочее место персонала подключено к открытым заказам.
- Заявки на скидку с согласованием и кнопкой запроса в рабочем месте персонала.
- VIP-комнаты с настраиваемыми минимумами депозита (по умолчанию 1 500 ₽ и 2 500 ₽).
- Закрытие VIP-заказа с расчётом subtotal, finalTotal и minimumAdjustment.
- PostgreSQL-схема, seed-данные, операционные индексы, Docker Compose и Nginx-конфигурация.
- Feature flags для ЕГАИС, «Честного знака», ККТ и ОФД.
- `smoke-test.ps1`: PASS (VIP, роли владельца и персонала, алиасы, split заказа, скидка, интеграции).
- `smoke-auth-required.ps1`: PASS (ограничения управляющего, доступ разработчика только на чтение финансов и склада, logout с отзывом сессии).
- `docker compose config`: PASS.
- Опубликована production-версия Sites v133 с панелью персонала (`/`) и панелью администратора (`/`) в тёмной теме с градиентными акцентами; деплой завершён успешно. В v131 усилена Velora-визуальная система, выровнен spacing-ритм, KPI сделаны кликабельными, а карточка сотрудника получила рабочее редактирование профиля: добавлены слоистый фон, аккуратные подсветки KPI, чёткие состояния навигации, hover/focus-состояния и более плотная композиция рабочих панелей. Адаптивный shell из v127 сохранён: зал и заказ персонала складываются в одну колонку, фильтры очереди переходят в сетку 2×2, а административные фильтры и кадровые формы не создают горизонтального переполнения.

## Порядок этапов

VPS — финальный этап после завершения интерфейсов, бизнес-логики и приёмочной проверки CRM. До этого система развивается и проверяется локально и в Sites.

## До VPS-релиза

- Запустить Docker Engine на целевом VPS и выполнить `deploy-vps.sh`.
- Задать боевые секреты и домен.
- Подключить HTTPS и резервное копирование PostgreSQL.
- Проверить миграцию таблицы `auth_sessions` на целевом VPS; split заказа, заявки/решения по скидкам и сессии уже поддерживают PostgreSQL с аудитом. Production-секрет администратора задаётся через `DEMO_ADMIN_PASSWORD`.
- Включить боевые секреты, серверную аутентификацию пользователей и аудит в БД на целевом VPS.
- Подключать российские интеграции отдельными адаптерами после согласования требований.


- Административные метрики доступны через `/api/metrics` и подключены к `admin.js`.

- Административные страницы `/inventory`, `/finance`, `/reservations` подключены к API: остатки и движения, финансовая сводка, создание и просмотр бронирований.
- Общий административный shell `portal.js` добавляет навигацию, KPI, поиск по складу, приход/расход, фильтр даты финансов и форму бронирования.
- API проверен smoke-сценарием: складское движение, создание брони и финансовая сводка проходят.
- Demo-авторизация реализована через `/api/login`; поддержан bearer-токен и режим `AUTH_REQUIRED=true` для VPS.
- Учётные записи из PostgreSQL теперь могут входить по логину и scrypt-хешу пароля; форма создания сотрудника принимает логин и временный пароль.
- Demo-fallback также создаёт локальную учётную запись с логином/паролем: проверено созданием бармена и повторным входом (`201` → `200`).
- Серверные permission-checks закрывают прямой доступ к заказам, броням, полу, каталогу и интеграциям по ролям.
- Добавлен журнал аудита `/api/audit` для создания/закрытия/разделения заказов, скидок, склада, броней и сотрудников.
- Последние события аудита выведены в админский обзор отдельным блоком.
- Добавлены PostgreSQL-репозитории для заказов, склада, бронирований и аудита; при наличии `DATABASE_URL` API использует их, demo fallback сохраняется без БД.
- `schema.sql` и `seed.sql` расширены пользователями, ингредиентами и начальными складскими остатками.
- Добавлены изображения товаров, аватары сотрудников, редактирование телефона/логотипа компании и деактивация сотрудников. VIP-депозит при бронировании проверяется сервером.
- Административные страницы используют единую тёмную Velora-дизайн-систему; склад, финансы и бронирования приведены к общей теме. Административная верстка имеет desktop/tablet/mobile breakpoints и сворачиваемую боковую навигацию; мобильная версия проверена на ширине 390 px без горизонтального переполнения.
- Очередь заказов и задач на рабочем месте фильтруется интерактивно; управляющий видит склад и финансы через согласованные permission-алиасы, а разработчик получает полный доступ к административному порталу и рабочему месту.
- Владелец и управляющий могут открыть временное рабочее место активного сотрудника в отдельной вкладке; исходная учётная запись и аудит действий сохраняются.
- Добавлены `deploy-vps.sh`, `backup-postgres.sh`, healthcheck CRM и production-заголовки Nginx.
- Добавлен `migrate-vps.sh` для применения всех версионированных миграций к существующему PostgreSQL-томe. `deploy-vps.sh` проверяет production-секреты до запуска контейнеров.
- Открытие и закрытие смены сохраняются в PostgreSQL при подключённом `DATABASE_URL`; добавлена миграция `002_orders_notes.sql` для существующих баз.
- Заметки заказа сохраняются через API и доступны во вкладке «Заметки» рабочего места.
- PostgreSQL-репозиторий заказов подключается автоматически при наличии `DATABASE_URL`; demo fallback сохраняется без БД.
- Production-пакет проверен через `npm pack --dry-run`; все 22 компонента включены.
- `npm ci` и `npm audit --omit=dev` проверены: зависимости устанавливаются, уязвимостей нет.

- Выход сотрудника очищает токен сессии и переводит на авторизацию даже после динамической отрисовки профиля.

- Загруженный логотип компании теперь отображается в административной навигации и шапке всех административных страниц.
- Административное меню разделено на «Главная», «Операции», «Контроль» и «Администрирование»; у главного разработчика добавлен отдельный экран диагностики и будущих интеграций; пункты «Персонал» и «Настройки» видны по назначенным правам, а управляющий ведёт кадровую мини-базу в своей зоне ответственности.
- Прямые ссылки административного портала дополнительно проверяют роль: запрещённая страница перенаправляет на разрешённый раздел.
- AUTH_REQUIRED приёмочный сценарий подтверждает доступ главного разработчика к персоналу, бронированиям и залу, а также просмотр склада и финансов без операций изменения.
- Владелец и управляющий могут создавать и блокировать рабочие учётные записи, редактировать контакты и кадровые поля; главный разработчик сохраняет доступ к CRM без паспортного блока и без записи в финансах/складе. Каталог и состояние интеграций показывают понятную ошибку при недоступности API.
Пароли рабочих учётных записей требуют минимум 6 символов.

Администратор может назначать рядовые и старшие роли; назначение управляющего или разработчика доступно только владельцу и фиксируется в аудите.

Последнее изменение: интерфейс персонала в статическом режиме теперь читает настроенные минимумы VIP-комнат из общего состояния CRM; серверный режим сохраняет получение этих значений через `/api/floor`.

- Разработчик видит финансы и склад в режиме просмотра; операции изменения остатков и согласования скидок доступны владельцу и управляющему.

- Добавлен `scripts/local-design-contract.mjs`: локально проверяет актуальную версию CSS, отсутствие подчёркивания action-ссылок и наличие Tabler Icons; контракт проходит.
- Повторный полный локальный приёмочный прогон на `http://localhost:3000` прошёл: маршруты, роли, инфографика, дата, ассеты, авторизация, каталог чая, CRUD, гости, 10 заказов и CRM smoke; итог `LOCAL ACCEPTANCE: PASS`.
- Браузерный визуальный QA после последнего коммита подтверждён на `/admin`, `/orders` и `/inventory`: action-кнопки без подчёркивания, единая тёмная тема, ровные карточки/формы и боковая навигация без горизонтального переполнения на desktop.
- Страница `/login.html` визуально проверена: фотографический фон кальяна, тёмная карточка, фирменный градиент, переключатель пароля и понятные поля авторизации отображаются согласованно с CRM.
- Добавлен GitHub Actions workflow `.github/workflows/crm-contracts.yml`: каждый push/PR запускает визуальные, security, role, insights, date, login и shell-контракты, аудит зависимостей и проверку production-пакета.
- GitHub Actions проверен фактическим запуском: run `35651251737` для коммита `f83adbb` завершён успешно; workflow поднимает локальный CRM перед API-контрактами.





- Визуальный QA 22.09.2026: нативные стрелки input[type=number] скрыты и заменены единым тёмным стилем CRM; CSS cache-busting обновлён до
ev=130.

- Рабочее место: фильтры «Все / Новый заказ / Готовится / Готово» переведены из визуальных span в полноценные кнопки с ria-pressed; фильтрация доступна мышью и клавиатурой.

- Зоны рабочего места («Зал», «Терраса», «VIP») получили tablist/tab и корректное aria-selected при переключении.

- Навигация рабочего места теперь обновляет ria-current=page вместе с активным пунктом меню.

- Кэш административной логики синхронизирован: все portal-страницы подключают `portal.js?rev=172`; добавлена проверка версии в design contract.
- Выпадающие меню portal-страниц переведены на единый доступный компонент: тёмный список с подсветкой выбранного значения, клавиатурной навигацией, Escape/кликом вне меню и сохранением нативного значения формы; скрытый источник исключён из accessibility-дерева, чтобы не было дублей.
- Открытие и закрытие смены в административной панели используют тематическое CRM-окно с пояснением, валидацией суммы, отменой и Escape вместо системного `prompt`.
- Редактирование и удаление залов, VIP-комнат и точек сети используют это же тематическое окно с описанием последствий; системные `prompt/confirm` из этих сценариев убраны.

- Исправлена последняя stale-ссылка: рабочее место подключает pp.js?rev=101; design contract теперь проверяет и эту версию.
## SaaS progress

- Added the multi-tenant foundation in migration `009_saas_foundation.sql`: organizations, memberships, subscriptions, venue/user organization links, and a bootstrap account for existing data.
- Added `GET /api/saas/account` for owner/admin diagnostics and `local-saas-contract.mjs`; local acceptance confirms plan, seat and venue limits.

## SaaS Control Center (2026-09-22)
Добавлена платформа владельца SaaS: роль platform_owner, /platform, реестр организаций и создание новых компаний. Операционные данные CRM остаются в tenant-контуре. Локальная проверка выполнена acceptance suite на отдельном порту; пароль SaaS-владельца хранится только в ignored .env.

Тарифный billing работает в режиме 	est_free: четыре тарифа, лимиты мест/заведений, переключение тарифа и аудит; денежные операции отключены.

- Адаптивная навигация: на мобильной ширине подписи бокового меню скрываются, но ссылки сохраняют title и aria-label с названием пункта для понятной подсказки и доступности (portal.js rev=172).

- Проверен AX-экран /admin: повторный статус в блоке смены устранён; шапка показывает глобальный статус, карточка использует «Касса активна» или «Касса не открыта» (portal.js rev=173).

- Мобильный QA при viewport 390×844 выявил переполнение блока «Ожидается» внутри карточки выручки; добавлена вертикальная мобильная раскладка карточки и обновлён CSS cache до rev=179.

- Планшетный QA при viewport 768×1024: шапка больше не обрезает имя; на средней ширине скрываются дата и подпись роли, сохраняются статус смены, уведомления и аватар (CSS rev=180).

- Healthcheck усилен: при DATABASE_URL /api/health выполняет SELECT 1 и возвращает 503 при недоступной PostgreSQL; memory/demo режим явно сообщает database: memory. Проверены оба ответа локально.

- Гостевые записи больше не маскируют ошибку PostgreSQL: создание и редактирование гостя возвращают 503 с кодом ошибки при сбое сохранения; при редактировании memory-состояние восстанавливается. Acceptance после изменения прошёл.

- Административные настройки VIP-минимумов и реквизитов заведения больше не возвращают успех при ошибке PostgreSQL; локальные значения восстанавливаются, API отвечает 503. Acceptance после изменения прошёл.

- SaaS-подписка и бонусы гостей: при ошибке PostgreSQL изменения откатываются в памяти и API отвечает 503 (subscription_save_failed / loyalty_save_failed), чтобы не показывать ложный успех; acceptance после изменения прошёл.

- Control Center: PATCH организации теперь сохраняет имя, тариф и активность в PostgreSQL, синхронизирует лимиты подписки и возвращает 503 при ошибке; memory/demo режим сохраняет прежний fallback.

- PATCH организации переведён на транзакцию PostgreSQL: имя, тариф, активность и лимиты подписки фиксируются атомарно; при сбое выполняется ROLLBACK. Acceptance повторно прошёл.

- Пакет VPS перепроверен локально: docker compose config --quiet и shell-синтаксис deploy-vps.sh, ackup-postgres.sh, erify-backup.sh, migrate-vps.sh, post-deploy-acceptance.sh проходят. Реальный запуск по-прежнему требует Docker Engine и целевого VPS.

- Добавлен отдельный local-platform-organization-contract.mjs: проверяет валидацию тарифа и безопасный атомарный PATCH организации; включён в полный acceptance и прошёл.

- Браузерная визуальная приёмка 22.09.2026: /finance и /inventory проверены на desktop 1440×900 и mobile 390×844; карточки, заголовки, действия и адаптивная сетка не перекрываются, выпадающие поля сохраняют единый стиль.

- Повторная проверка зависимостей 22.09.2026: 
pm ci --ignore-scripts --no-audit завершён, 
pm audit --omit=dev --audit-level=high — 0 уязвимостей.

- Рабочее место теперь защищается экраном блокировки: для сотрудника с настроенным 4-значным PIN автоматическая блокировка срабатывает после 5 минут бездействия, а кнопка «Заблокировать» доступна вручную в шапке. Разблокировка поддерживает клавиатуру и экранную цифровую панель; неверный PIN отклоняется, а аккаунт без PIN не получает ложный доступ. `local-lock-contract.mjs` и полный `local-acceptance.ps1` прошли.
- Экран блокировки переработан в стеклянно-ледяном стиле: холодные полупрозрачные слои, кристаллические блики, мягкое размытие и контрастная PIN-панель. В шапке добавлены отдельные кнопки блокировки и настроек.
- Автоблокировка теперь персональная: каждый пользователь выбирает «Выключена», 1, 5, 10, 15 или 30 минут. Выбор хранится по идентификатору пользователя в его профиле браузера; ручная блокировка доступна всегда.
- Пользовательские настройки теперь сохраняются в аккаунте: добавлена миграция `011_user_preferences.sql` и `/api/session/preferences`. Таймер автоблокировки синхронизируется с сервером между устройствами, валидируется и откатывается при ошибке БД.
- Полный acceptance после добавления хранения предпочтений прошёл, включая новый `local-preferences-contract.mjs`.
- Актуализация документации: текущие значения cache-busting — `style.css` rev=182, `portal.js` rev=173, `app.js` rev=109; click contract подтверждает 113 ссылок, 150 кнопок и 4 формы. Исторические записи предыдущих ревизий сохранены как журнал изменений.
- Проверка Docker CLI 22.09.2026: CLI/Compose доступны только для статической проверки конфигурации; локального Docker-контура нет. Реальная сборка, PostgreSQL restart и backup restore выполняются на целевом VPS.
- Главная и аналитика переведены на серверные предпочтения аккаунта: видимость модулей, стиль показателя выручки и набор показателей сохраняются через `/api/session/preferences`, при этом локальная настройка остаётся резервом для demo-режима.
- Полный acceptance после синхронизации dashboard/insights preference прошёл на отдельном локальном порту: `LOCAL ACCEPTANCE: PASS`.
